# Gemini API Key (if utilized)
GEMINI_API_KEY=

# Supabase Configuration (Optional for cloud sync - app operates with local IndexedDB safety layer if not configured)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

